footer
	.container
		.footer
			.footer__button
				button.main-button 
					span.main-button__text FIND THE PLACE
			.footer__nav
				ul.nav__list-footer
						each item in links
							li.nav__item-footer
								a.nav__link-footer(href=`#${item}`) #{item}