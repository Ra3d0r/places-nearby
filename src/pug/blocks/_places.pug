.section-places
	.container
		#root
			.section-places__grid-container
				//- +place
			//- .pagination 
				- for(let i = 1; i <= 8; i++)
					input(id='toggle-' + i, name="toggle", type="radio")
					label(class='label-' + i, for='toggle-' + i)= i

